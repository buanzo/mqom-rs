[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
aes = { version = "0.9.2", default-features = false }
mqom = { path = ".." }
rand_core = { version = "0.10.1", default-features = false }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"
